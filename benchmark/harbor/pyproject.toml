[project]
name = "motus-agent"
version = "0.1.0"
description = "MotusAgent: agent for the Harbor (terminal-bench) benchmark, built on Motus."
requires-python = ">=3.12"

dependencies = [
    "lithosai-motus",
    "python-dotenv",
    "pyyaml",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["motus_agent*"]

[tool.setuptools.package-data]
"motus_agent" = [
    "skills/**/*.md",
    "agent_prompts/**/*.diff",
]
