diff --git a/benchmarks/harbor/actus_bot/agent_prompts/executor/prompt.py b/benchmarks/harbor/actus_bot/agent_prompts/executor/prompt.py
index a912e7a..3a57e3e 100644
--- a/benchmarks/harbor/actus_bot/agent_prompts/executor/prompt.py
+++ b/benchmarks/harbor/actus_bot/agent_prompts/executor/prompt.py
@@ -57,6 +57,18 @@ Executing...
 - Skip obvious steps (don't list "read the file" separately)
 - For very simple tasks (single obvious change): 1-2 bullets is fine
 
+**PROGRAMMING TASK APPROACH**
+
+When writing a program, if the task is very large, use incremental construction:
+
+1. CREATE SKELETON FIRST
+- Write: imports, function signatures with pass/empty bodies, main entry point
+2. IMPLEMENT FUNCTIONS A FEW AT A TIME
+- Writable sizable chunks of code at a time up
+- Repeat until the task is complete
+Always: skeleton first, then build piece by piece.
+
+
 ## Output Format
 
 Brief completion report:
